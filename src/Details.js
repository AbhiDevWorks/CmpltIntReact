import React from "react";

const Details = props => {
  return <h1>hi!</h1>;
  {
    /* AWESOME FOR DEBUGGING PROPS
      <pre>
      <code>{JSON.stringify(props, null, 4)}</code>
    </pre> */
  }
};

export default Details;
